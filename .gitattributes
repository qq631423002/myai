# 换行符统一策略（协作必备）
#
# 为什么需要这个文件：
#   Windows 上的 Git 默认会把换行符在 LF 和 CRLF 之间转来转去，
#   如果两个人的 core.autocrlf 设置不一样，就会出现
#   「我没改这个文件，但 Git 说整个文件都被改了」的假 diff ——
#   几百行红绿一片，真正的改动完全看不出来。
#
# 解决办法：在仓库里统一存成 LF，检出时按平台处理。
* text=auto

# 二进制文件不要做换行符转换（转了文件就坏了）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
*.ttf binary
*.onnx binary
*.db binary

# 锁文件别做 diff（太长，没有阅读价值）
pnpm-lock.yaml -diff
